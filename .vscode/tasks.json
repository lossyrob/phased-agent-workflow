{
  "version": "2.0.0",
  "tasks": [
    {
      // Compile the TypeScript extension once. This task is referenced by the
      // launch configurations to ensure the extension is built before debugging.
      "type": "npm",
      "script": "compile",
      "path": "vscode-extension/",
      "group": "build",
      "problemMatcher": "$tsc",
      "label": "npm: compile - vscode-extension",
      "detail": "tsc -p ./",
      "presentation": {
        "reveal": "silent"
      }
    },
    {
      // Watch mode: continuously recompile TypeScript on file changes.
      // Useful for iterative development. Start this task manually with
      // Terminal > Run Task > "npm: watch - vscode-extension"
      "type": "npm",
      "script": "watch",
      "path": "vscode-extension/",
      "group": "build",
      "problemMatcher": "$tsc-watch",
      "label": "npm: watch - vscode-extension",
      "detail": "tsc -watch -p ./",
      "isBackground": true,
      "presentation": {
        "reveal": "silent"
      }
    }
  ]
}
